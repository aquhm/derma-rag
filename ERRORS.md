# ERRORS.md

두 번 이상 실패한 접근을 기록한다. 같은 자리에서 다시 넘어지지 않기 위한 실수 노트다.

형식: `E-### 증상 / 시도한 것 / 통한 것 / 교훈`

---

## 기록

### E-000 · 템플릿

**증상** 무엇이 어떻게 실패했는가. 오류 메시지가 짧으면 그대로 인용한다.
**시도한 것** 어떤 접근을 몇 번 시도했는가.
**통한 것** 결국 무엇으로 해결했는가. 해결 못 했으면 "미해결"이라고 적는다.
**교훈** 다음에 같은 상황에서 무엇을 먼저 볼 것인가.

---

### E-001 · gemma4:31b가 생성 모델로 못 쓸 만큼 느리고 지시를 어긴다

**증상** 짧은 한국어 질문 하나에 **189초**가 걸렸다. "한 문장으로만 답하세요"라는 지시를 무시하고 **1075 토큰**을 생성했으며, 응답이 한국어가 아니라 영어였고 내용도 질문과 무관했다("I'm sorry, but your message appears to be...").

측정값 (`scripts/bench_models.py`, 2026-09-07):

1차 (`num_predict` 무제한, "한 문장으로만"):

| 모델 | 응답 시간 | 생성 토큰 | 초당 토큰 | 지시 준수 | 언어 |
|---|---|---|---|---|---|
| `gemma4:31b` | 189.3s | 1075 | 5.7 | 무시 | 영어 |

2차 (동일 프롬프트, `num_predict=200`, `temperature=0.6`):

| 모델 | 응답 시간 | 생성 토큰 | 초당 토큰 | 응답 내용 |
|---|---|---|---|---|
| `gemma4:31b` | 46.8s | 200 (상한 도달) | **4.3** | **비어 있음** |
| `qwen2.5:3b` | 6.0s | 85 | **14.1** | 정상 한국어 2문장 |

2차에서 `gemma4:31b`는 200토큰 상한을 다 쓰고도 `strip()` 후 남는 텍스트가 없었다. 공백이나 개행만 생성한 것으로 보인다. **느린 것만이 문제가 아니라 쓸 수 있는 답이 나오지 않는다.**

**원인** `gemma4:31b`는 19GB로 VRAM 16GB를 초과한다. Ollama가 RAM으로 스필시켜 동작은 하지만 GPU 밖에서 도는 부분이 병목이 된다. 지시 무시와 영어 응답은 별개 문제로, 이 모델의 한국어 지시 추종이 약한 것으로 보인다.

**통한 것** `qwen2.5:3b`로 교체. **31배 빠르고**, 한국어로 답하며, "두 문장으로만"이라는 지시를 지켰다.

**교훈** VRAM을 초과하는 모델은 "느리지만 된다"가 아니라 **실질적으로 못 쓴다**. 대화형 RAG에서 응답 하나에 3분은 사용 불가다. 모델을 고를 때 크기보다 **VRAM 안에 들어가는지**를 먼저 본다. 어댑터 교체가 `composition.py` 한 줄이라 이 전환에 비용이 들지 않았다 — 레이어 분리(D-009)의 첫 번째 실효다.

### E-002 · 한국어 고정 문장으로 "자료 없음"을 판정하려다 근거 없는 답이 나갔다

**증상** `AnswerQuestion`이 무관한 질문("자동차 엔진 오일은 몇 km마다 교환해야 하나요?")에 `Refusal`이 아니라 `Answer`를 돌려줬다. 본문은 "제공된 자료에 자동차 엔진 오일 교환에 관한 내용이 없습니다."였고 근거 5건이 함께 붙어 나갔다.

**원인** 프롬프트가 모델에게 "제공된 자료에 해당 내용이 없습니다"라고 답하라고 지시하고, 코드는 그 문장을 문자열 포함 검사로 찾았다. `qwen2.5:3b`는 지시한 문장을 그대로 쓰지 않고 질문의 단어를 끼워 넣어 바꿔 썼다. 문자열 비교가 어긋나 정상 답변으로 처리됐다.

**통한 것** 모델이 출력할 신호를 짧은 고정 토큰 `NO_ANSWER`로 바꿨다. 여기에 한국어로 풀어 쓴 경우를 잡는 보조 정규식을 더했다. 정규식은 주어를 "자료·문단·문서"로 제한한다. "없습니다"만 보면 "알코올이 없습니다" 같은 정상 답변까지 거부한다. 실제 스택 확인은 `tests/application/test_answering_end_to_end.py`에 남겼다.

**교훈** 모델에게 자연어 문장을 그대로 반복하라고 시키고 그 문장을 코드가 비교하지 않는다. 자연어는 모델이 바꿔 쓴다. 기계가 판정할 신호는 짧고 형태가 고정된 토큰으로 시킨다. 그리고 프롬프트 지시는 지켜지지 않을 수 있으므로 판정 코드에 보조 검사를 둔다 (SAFETY.md의 "프롬프트가 아니라 코드로 막는다").


### E-003 · 생성 타임아웃 한 건이 200건짜리 평가를 통째로 죽였다

**증상** 기준선 측정(표본 200건) 중 답변 채점 1건째에서 `GenerationUnavailable: 생성 요청 실패 (model=qwen2.5:3b): HTTPConnectionPool(host='localhost', port=11434): Read timed out. (read timeout=300.0)`. 검색 채점 200건은 이미 끝난 상태였고, 그 결과까지 함께 잃었다. 종료 코드 2.

**시도한 것** 같은 타임아웃이 그 전 실험(색인 구성 비교)에서도 한 번 났다. 두 번째다.

**원인** 확정하지 못했다. 실패 직후 단건 생성은 2.7초에 끝났고 `ollama ps`는 `qwen2.5:3b`가 100% GPU에 올라가 있음을 보여 줬다. 임베딩(`bge-m3`)과 생성(`qwen2.5:3b`)을 케이스마다 번갈아 호출하는 구간에서 났으므로 모델 적재 경합이 의심되지만 재현하지 못했다.

**통한 것** 원인을 없애는 대신 **한 건의 실패가 전체를 죽이지 않게 고쳤다.** `EvaluateAnswers`가 케이스별로 예외를 잡아 `failures`에 케이스 ID와 예외 종류만 남기고 계속 진행한다. 실패 건수는 결과 JSON의 `failed_count`로 나가고, 거부율 분모에서 빠진다. 실패는 시스템 장애고 거부는 정상 동작이라 섞으면 안 된다.

**교훈** 오래 걸리는 배치는 **부분 실패를 견디게** 만든다. 20분짜리 실행이 한 건 때문에 0이 되면 재시도 비용이 실패 비용보다 크다. 적재 로더(9,031개 파일)에 같은 처리를 이미 해 뒀는데 평가 쪽에는 빠져 있었다. 같은 성격의 코드에 같은 규칙을 적용했는지 확인한다.


### E-004 · 실험 구성과 배포 구성이 달랐다

**증상** M3-6 실험에서 QA쌍 색인이 F1 0.315로 좋게 나와 채택했다. 배포 코드로 색인을 만들었더니 청크가 8,831개가 아니라 **20,681개**였다. 스모크 테스트에서 근거 3건 중 2건이 같은 QA(`A036219_07_QA1#0`, `#1`)인 것이 눈에 띄어 발견했다.

**원인** 실험 스크립트는 QA 1건을 `Chunk` 하나로 직접 만들었다. 배포 경로는 `ParagraphChunker`를 거치고, QA 텍스트가 평균 855자라 512자 한계에 걸려 2~3조각으로 쪼개졌다. 쪼개지면 상위 k를 같은 QA의 조각들이 차지해 실질 근거 수가 줄어든다.

**통한 것** `WholeDocumentChunker`를 만들어 QA 재료에는 청킹을 적용하지 않게 했다. 청크가 9,031개가 되고 근거 3건이 서로 다른 QA로 채워졌다.

**교훈** 실험 스크립트가 파이프라인을 우회하면 **측정한 것과 배포하는 것이 달라진다.** 채택 전에 배포 경로로 한 번 돌려 산출물 규모(건수·청크 수)를 실험치와 대조한다. 숫자만 보지 말고 실제 출력도 한 번 눈으로 본다. 이번에는 근거 목록을 눈으로 봤기 때문에 잡혔다.


---

## 착수 전에 알고 있는 위험

아직 겪지 않았지만 예상되는 실패 지점이다. 실제로 겪으면 위 형식으로 옮겨 적는다.

### 데이터 형식 — 2026-09-08 확인 완료

`.doc` 6,170개가 **한 형식이 아니다.** OLE2 구형 5,446개, ZIP(실제 `.docx`) 718개, RTF 4개, 빈 파일 2개다. 확장자만 보고 파서를 고르면 5,446개에서 실패한다.

지식데이터 `.json` 3,090개에는 본문이 없다. 제목·저자·출처·키워드뿐이다.

**우회했다.** QA 라벨링 JSON이 근거 원문을 그대로 담고 있어(고유 5,921건) `.doc` 파싱 없이 색인할 수 있다 (`MEMORY.md` D-027). `.doc` 원문 색인은 M3 실험 항목으로 미뤘다. 그때는 OLE2 변환 수단이 필요하다.

### 한글 인코딩

Windows 환경에서 파일을 읽고 쓸 때 인코딩을 명시하지 않으면 CP949로 처리되어 깨진다. `open()` 호출마다 `encoding="utf-8"`을 명시한다. 표준 출력도 마찬가지다.

### Ollama 임베딩 배치

`nomic-embed-text`로 9,377건을 한 번에 처리하려 하면 시간이 오래 걸리거나 중간에 끊긴다. 배치 처리와 중간 저장(체크포인트)을 넣지 않으면 실패 시 처음부터 다시 해야 한다.

### ~~gemma4:31b 스필~~ → E-001로 실현됨

예상보다 나빴다. 수십 초가 아니라 **189초**였다. E-001 참조.

### 청크 경계

문단을 자를 때 문맥이 끊기면 검색은 되는데 답이 이상해진다. 검색 정확도(Recall@k)는 높은데 답변 정확도가 낮으면 청크 경계를 먼저 의심한다. 오버랩을 넣지 않은 경우가 흔하다.

### 평가 지표 혼동

검색이 틀린 것과 생성이 틀린 것을 구분하지 않으면 고칠 곳을 못 찾는다. Recall@k와 답변 정확도를 반드시 따로 측정한다(MEMORY.md D-008).
