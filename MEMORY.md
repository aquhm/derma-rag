# MEMORY.md

이 프로젝트에서 이미 내린 결정을 기록한다. 결정을 뒤집자는 제안이 나오면 해당 `D-###`의 근거를 먼저 확인한다.

형식: `D-### 결정 / 근거 / 뒤집을 조건`

이 파일이 담는 것은 **결정과 그 근거**다. 무엇을 언제 만들었는지는 `docs/ROADMAP.md`, 무엇이 실패했는지는 `ERRORS.md`에 있다.

---

## 결정 색인

| ID | 결정 | 관련 |
|---|---|---|
| D-001 | 프로젝트 목적은 학습이다 | 전체 |
| D-002 | 파인튜닝이 아니라 RAG로 간다 | 전체 |
| D-003 | LangChain과 LlamaIndex를 쓰지 않는다 | 전체 |
| D-004 | 벡터 DB를 쓰지 않는다 | M1-5 |
| D-005 | 임베딩과 생성은 Ollama 로컬로 한다 | M0-6 |
| D-006 | 원본 데이터를 저장소 밖으로 내보내지 않는다 | 전체 |
| D-007 | 검색 실패 시 생성 단계로 넘기지 않는다 | M1-6, M1-7 |
| D-008 | 평가는 내장 QA 10,043쌍으로 한다 | M2 |
| D-009 | 레이어 아키텍처를 적용한다 | 전체 |
| D-010 | 문서 우선으로 진행한다 | 전체 |
| D-011 | AIHub-MCP를 붙이지 않는다 | M0 |
| D-012 | 값 객체가 불변식을 강제한다 | M1-1 |
| D-013 | `VectorIndex` 포트에 `load`를 두지 않는다 | M1-2 |
| D-014 | 샘플 문서는 직접 지어내고 `samples/`에 커밋한다 | M1-3 |
| D-015 | 청크 위치는 필드가 아니라 ID로 표현한다 | M1-3 |
| D-016 | 임베딩은 `/api/embed` 배치로 호출한다 | M1-4 |
| D-017 | Ollama 장애는 `Refusal`이 아니라 예외다 | M1-4 |
| D-018 | 색인은 `.npz` 파일 하나에 저장한다 | M1-5 |
| D-019 | 근거 충분성은 임계값 이상 근거의 존재 여부로 판정한다 | M1-6 |
| D-020 | "자료에 답이 없다"는 신호는 고정 토큰으로 받는다 | M1-6, M1-7 |
| D-021 | min_score=0.35는 현재 게이트로 동작하지 않는다. 실데이터로 다시 정한다 | M1-7 · **미해결** |
| D-022 | 생성 temperature 기본값은 0.0이다 | M1-6 |
| D-023 | `src/cli.py`는 얇은 진입점이고 본체는 `src/interface/cli.py`다 | M1-8 |
| D-024 | 데이터 검사 도구는 집계값만 출력한다 | M2-1 |
| D-025 | 평가 입력 형식을 먼저 정하고, AI Hub QA쌍은 그 형식으로 변환한다 | M2-5 |
| D-026 | 답변 정확도는 음절 2-gram F1로 잰다 (M2-7) | M2-7 |
| D-027 | 색인 대상은 QA 라벨링의 근거 발췌다 | M2-2 |
| D-028 | 피부 유형은 4대 분류와 세부 유형을 함께 붙인다 | M2-3 |
| D-029 | Recall@k를 기준선 지표에서 뺀다 | M2-6 |
| D-030 | k=3을 기본값으로 한다. 유사도 임계값은 조정하지 않는다 | M3-3 |
| D-031 | 메타데이터 필터링을 넣지 않는다. 개선 여지는 검색이 아니라 생성에 있다 | M3-2 |
| D-032 | 색인 재료를 QA쌍으로 바꾼다. QA는 쪼개지 않는다 | M3-6 |
| D-033 | 생성 모델은 qwen2.5:3b를 유지한다 | M3-5 |
| D-034 | F1은 실제 품질보다 비관적이다. 안전 규칙에 의료 이용 판단을 추가한다 | 실사용 확인 |
| D-035 | 논문 원문을 색인 재료로 쓰지 않는다. QA쌍 색인을 유지한다 | M3-7 |
| D-036 | LlamaIndex를 도입하지 않는다. D-003을 측정으로 확인했다 | M4-2 |

---

## 결정 기록

### D-001 · 프로젝트 목적은 학습이다
**결정** 상용 서비스가 아니라 학습용으로 만든다. 각 단계를 이해하는 것이 완성 속도보다 우선한다.
**근거** 사용자가 Python과 RAG를 처음 다룬다. "하나하나 단계를 이해하고 하면 좋겠다"고 명시했다.
**뒤집을 조건** 사용자가 배포 의사를 밝힐 때. 그 시점에 `docs/process/SAFETY.md`의 공개 배포 항목이 전부 적용된다.

### D-002 · 파인튜닝이 아니라 RAG로 간다
**결정** 모델 가중치를 건드리지 않는다. 검색과 프롬프트 조립으로 해결한다.
**근거** 파인튜닝은 GPU와 시간이 들고 답의 출처를 추적할 수 없다. RAG는 창고만 바꾸면 되고 답마다 근거 문단을 함께 보여줄 수 있다. 의료 인접 주제에서 근거 추적은 선택이 아니라 요건이다.
**뒤집을 조건** RAG로 도달할 수 없는 품질 요구가 생기고, 그것이 측정으로 증명될 때.

### D-003 · LangChain과 LlamaIndex를 쓰지 않는다
**결정** 6단계를 직접 구현한다. RAG 프레임워크를 도입하지 않는다.
**근거** 프레임워크는 6단계를 함수 두세 개 뒤로 숨긴다. D-001(학습 목적)과 정면으로 충돌한다. 핵심 로직은 100~150줄이며 이 규모에서 프레임워크의 이득이 없다. 참고로 측정된 비용도 있다 — 같은 RAG에 LangChain은 코드 30~40%를 더 쓰고, 직접 호출 p50 28ms 대비 LangChain 리트리버는 76ms다.
**뒤집을 조건** ~~직접 구현으로 6단계를 완주한 뒤, 검색 품질 개선을 위해 계층적 청킹이나 재랭킹이 필요해질 때.~~ **M4-2에서 실제로 재 봤다(D-036).** 같은 조건에서 F1 차이가 -0.0028로 노이즈 안이었고, 의존성이 2개에서 70개로 늘었다. 결정 유지. 다음 검토 시점은 재랭킹·하이브리드 검색이 필요해질 때다(M4-3). LangChain의 강점은 에이전트 오케스트레이션(LangGraph)이고 이 프로젝트는 에이전트가 아니다.

### D-004 · 벡터 DB를 쓰지 않는다
**결정** Chroma·FAISS·LanceDB를 도입하지 않는다. numpy 배열에 올리고 전수 코사인 유사도로 검색한다.
**근거** 지식데이터 9,377건 × 768차원 float32 = 약 28MB다. 전수 검색이 밀리초 단위로 끝난다. 벡터 DB는 수십만 건 이상에서 의미가 생긴다.
**뒤집을 조건** 문서 수가 10만 건을 넘거나, 검색 지연이 측정상 문제가 될 때.

### D-005 · 임베딩과 생성은 Ollama 로컬로 한다
**결정** 임베딩은 **`bge-m3`(1024차원)**, 생성은 **`qwen2.5:3b`**를 Ollama HTTP API로 호출한다.
**근거** 이미 설치돼 있어 마찰이 0이다. 데이터가 로컬을 벗어나지 않으므로 AI Hub 이용약관과 개인정보 측면에서 안전하다.
**2026-09-07 개정** 원래 생성 모델은 `gemma4:31b`였다. 실측 결과 **응답 하나에 189초**가 걸렸고 한국어 지시를 무시했다. `qwen2.5:3b`는 같은 프롬프트에 **6.0초**, 한국어 준수. 31배 차이다. 측정 상세는 `ERRORS.md` E-001.
**원칙** 모델 선택은 크기가 아니라 **VRAM 16GB 안에 들어가는지**로 판단한다. 초과 모델은 RAM 스필로 동작만 할 뿐 실사용이 안 된다.
**미결** `qwen2.5:3b`는 빠르지만 3B라 품질 상한이 낮을 수 있다. VRAM에 들어가는 중간 크기 모델(8~14B, 양자화 시 8~10GB)을 받아 비교하는 것이 다음 후보다. 다만 **기준선(M2-8)을 만든 뒤에** 바꾼다. 기준선 없이 모델을 바꾸면 개선 여부를 판단할 수 없다.
**2026-09-08 개정 (임베딩)** `nomic-embed-text`를 `bge-m3`로 바꿨다. nomic은 한국어 질문과 문단의 관계를 잡지 못했다. 같은 40쌍에서 질문이 정답 근거에 더 가까운 비율이 nomic 17/40(동전 던지기 이하), bge-m3 25/40이었다. 550문서 코퍼스 Recall@5는 nomic 0.0%, bge-m3 2.0%(무작위 기대 0.9%)였다. 답변 정확도는 F1 0.213 대 0.229, 거부는 3/20 대 1/20이었다. 임베딩 속도는 약 1.7배 느리고(80건 3.2초 대 5.5초) 차원이 768에서 1024로 늘었다.
**뒤집을 조건** 기준선 측정에서 생성 품질이 부족한 것으로 확인될 때.

### D-006 · 원본 데이터를 저장소 밖으로 내보내지 않는다
**결정** `data/`는 `.gitignore` 대상이다. 원본과 단순 가공본을 커밋·공유·업로드하지 않는다. 클라우드 API에 원문을 보내는 것도 검토 대상에 포함한다.
**근거** AI Hub 이용약관이 원본 데이터의 외부 공개·공유·재배포를 금지한다. 단순 가공(편집·수정) 형태의 공유도 포함된다. 학습 결과물의 활용은 자유이나 원본 자체는 아니다.
**뒤집을 조건** 없음. 이용약관 사항이다.

### D-007 · 검색 실패 시 생성 단계로 넘기지 않는다
**결정** 5단계 검색에서 임계값 이상의 근거를 얻지 못하면 6단계를 실행하지 않고 "답할 수 없음"을 반환한다.
**근거** 근거 없이 생성하면 모델이 지어낸다. 민감성·염증성 피부 주제에서 지어낸 제품 추천은 실제 악화로 이어질 수 있다.
**구현** 이 규칙은 프롬프트 지시가 아니라 **코드 분기**로 강제한다. 프롬프트는 어길 수 있고 분기는 못 어긴다.
**뒤집을 조건** 없음.

### D-008 · 평가는 내장 QA 10,043쌍으로 한다
**결정** 별도 평가셋을 만들지 않는다. 데이터셋에 포함된 질의응답쌍을 정답지로 쓴다. 검색 정확도(Recall@k)와 답변 정확도를 분리해 측정한다.
**근거** 정답지가 이미 있다. 분리 측정을 하는 이유는, 답이 틀렸을 때 원인이 검색에 있는지 생성에 있는지를 구분해야 고칠 곳을 알 수 있기 때문이다.
**2026-09-08 개정** 뒤집을 조건이 실제로 발생했다. Recall@k는 이 데이터셋에서 측정할 수 없다. 근거는 D-029. 답변 정확도(D-026)와 거부율만 남는다.

### D-009 · 레이어 아키텍처를 적용한다
**결정** domain / application / infrastructure / interface 네 레이어로 나눈다. 의존 방향은 안쪽으로만 향한다.
**근거** 임베딩 모델과 생성 모델을 교체할 가능성이 높다(D-005 주의 참조). 포트와 어댑터로 분리해 두면 교체가 어댑터 하나 추가로 끝난다. 학습 목적상 "왜 이렇게 나누는가"를 실제로 겪어 보는 것도 목적이다.
**강제** `scripts/check_layers.py`가 PostToolUse hook으로 자동 실행된다. 규칙 위반은 사람의 주의력이 아니라 스크립트가 잡는다.
**뒤집을 조건** 없음. 단 레이어 수를 줄이자는 제안은 검토 가능하다.

### D-010 · 문서 우선으로 진행한다
**결정** 코드보다 문서를 먼저 만든다. PRD, ARCHITECTURE, 레이어 지침, 안전 규칙을 세운 뒤 구현에 들어간다.
**근거** 사용자가 명시적으로 요청했다. 하네스 관점에서도 규칙이 없는 상태로 코드를 쓰면 매 세션 같은 설명을 반복하게 된다.
**뒤집을 조건** 없음.

### D-011 · AIHub-MCP를 붙이지 않는다
**결정** `github.com/aihub-git/AIHub-MCP`를 연결하지 않는다. 이 프로젝트는 MCP를 하나도 쓰지 않는다.
**근거** 네 가지다.
1. **필요한 기능이 없다.** 이 MCP는 메타데이터 조회·검색 전용이며 **실제 데이터 다운로드가 불가능**하다. 제공 도구는 `getDataSet`, `describeDataSet`, `searchDataSets`, `countDataSets`, `getDataSetsWithGuide` 5종이다. 우리는 대상 데이터셋(71961)이 이미 확정돼 있어 탐색이 필요 없다.
2. **라이선스가 미기재다.** 사용 조건이 불명확하다.
3. **검증되지 않았다.** 커밋 4개, 스타 2개, 포크 1개. 제7장이 경고하는 "third-party MCP의 correctness와 security를 직접 검증해야 한다"에 걸린다.
4. **마찰이 크다.** Java 17 + JAR 실행 + `AIHUB_API_KEY` 환경변수가 필요하다. 얻는 것에 비해 설치 비용이 크다.
**뒤집을 조건** 다른 AI Hub 데이터셋을 폭넓게 탐색해야 할 일이 생기고, 그때까지 이 저장소에 라이선스가 명시되며 유지보수 이력이 쌓였을 때.

### D-012 · 값 객체가 불변식을 강제한다
**결정** `domain`의 값 객체는 단순 데이터 컨테이너가 아니다. `__post_init__`에서 불변식을 검사하고, 어기면 생성 자체를 실패시킨다. 핵심은 두 가지다. 근거가 빈 `Answer`는 만들 수 없고(`ValueError`), 면책 문구는 필드가 아니라 `text` 프로퍼티가 붙여서 부착을 건너뛸 경로가 없다.
**근거** 이 프로젝트의 안전 규칙은 "프롬프트 지시가 아니라 코드 분기"로 강제한다(`docs/process/SAFETY.md` S-2). 값 객체만 예외로 두면 일관성이 없다. D-007(근거 없으면 생성 안 함)과 S-5(면책 항상 부착)가 `application`의 `if` 문 하나에만 의존하지 않고 타입 수준에서도 막힌다. 게이트가 이중이 되므로 새 호출 경로가 생겨도 뚫리지 않는다.
**비용** `frozen=True`에서는 `__post_init__` 안에서 값을 대입할 수 없다. 검증만 하고 값은 바꾸지 않는 형태로 쓴다. `Answer.evidence`는 `list`가 아니라 `tuple`만 받는다. `list`를 두면 `append()`로 내용이 바뀌어 불변이 이름뿐이 된다.
**뒤집을 조건** 검증이 테스트 작성을 방해하는 것이 실제로 확인될 때. 그 경우 팩토리 메서드(`Answer.create()`)로 검증을 옮기고 생성자를 열어 둔다.

### D-013 · `VectorIndex` 포트에 `load`를 두지 않는다
**결정** 포트는 `add`, `search`, `save` 세 개만 갖는다. 색인을 파일에서 읽는 것은 어댑터의 classmethod `NumpyVectorIndex.from_file(path)`가 맡고, 그 호출은 `composition.py`에서만 일어난다.
**근거** 포트에 `load(path)`를 두면 "아직 로드되지 않은 빈 색인" 상태가 생긴다. 그 상태에서 `search()`가 무엇을 반환해야 하는지는 답할 필요가 없는 질문이다. 생성 시점에 이미 완성된 객체만 존재하게 만들면 그 질문 자체가 사라진다. D-012와 같은 원칙이다.
**부수 효과** `docs/ARCHITECTURE.md` 3절의 포트 코드에서 `load` 줄을 지웠다. 5절 조립 예시가 이미 `from_file`을 쓰고 있어 두 곳이 어긋나 있었고, 이 결정으로 맞춰졌다.
**뒤집을 조건** 색인을 다시 읽어 같은 인스턴스를 갱신해야 하는 요구가 생길 때. 지금은 그런 경로가 없다.

---

### D-014 · 샘플 문서는 직접 지어내고 `samples/`에 커밋한다
**결정** M1의 샘플 데이터 10건은 AI Hub 원본이 아니라 이 프로젝트가 지어낸 문장이다. `samples/documents.json`에 두고 저장소에 커밋한다. `data/`에 넣지 않는다.
**근거** `data/`는 `.gitignore` 대상이고 `guard_data.py` 훅이 쓰기를 막는다(D-006). 그렇다고 샘플을 코드 안 상수로 두면 파일을 읽는 코드가 M2까지 생기지 않는다. M1-8의 `ingest` 명령이 실제로 파일을 읽어야 하고, Windows에서 `encoding="utf-8"`을 빠뜨렸을 때 한글이 깨지는 것도 지금 겪는 편이 낫다.
**주의** `samples/`에 AI Hub 원문을 넣지 않는다. 넣는 순간 D-006 위반이다. 메타데이터 라벨 체계(4대 분류, 부위)만 실제와 맞춘다.
**뒤집을 조건** 없음. 실데이터가 들어와도 `samples/`는 테스트와 데모용으로 남긴다.

### D-015 · 청크 위치는 필드가 아니라 ID로 표현한다
**결정** `Chunk`에 순번 필드를 두지 않는다. ID를 `{doc_id}#{순번}` 형식(`sample-007#1`)으로 만들어 순서를 담는다.
**근거** 순번은 청크를 만들 때 결정되고 이후 바뀌지 않는다. 별도 필드로 두면 ID와 순번이 어긋날 수 있는 상태가 생긴다. ID 하나만 보고 어느 문서의 몇 번째인지 알 수 있는 편이 로그에서도 유리하다. 로그에는 원문 대신 청크 ID만 남기기 때문이다(`00-common.md`).
**비용** 순번으로 정렬하려면 ID를 파싱해야 한다. 지금은 그런 경로가 없다.
**뒤집을 조건** QA쌍 채점(M2-5)에서 근거 문단을 순번으로 대조해야 하고, 파싱이 실제로 불편한 것으로 확인될 때.

---

### D-016 · 임베딩은 `/api/embed` 배치로 호출한다
**결정** Ollama의 `POST /api/embed`를 쓴다. 한 요청에 64건을 보낸다. 구형 `POST /api/embeddings`(단건)를 쓰지 않는다.
**근거** 2026-09-07 실측(Ollama 0.33.3, `nomic-embed-text`, 모델 예열 후). 같은 50건에 배치 1회는 **2.63초**, 단건 루프 50회는 **104.56초**로 39.7배 차이가 났다. HTTP 왕복 비용이 임베딩 계산보다 크다. 9,377건으로 환산하면 약 8분 대 약 5시간 24분이다. M2-4 전체 임베딩이 실현 가능한지가 여기서 갈린다.
**부수 효과** `docs/guidelines/03-infrastructure.md`의 어댑터 표가 `/api/embeddings`로 적혀 있었다. `/api/embed`로 고쳤다.
**관찰** `/api/embed`가 돌려주는 `nomic-embed-text` 벡터는 이미 L2 정규화돼 있다(norm = 1.0). 그래도 `NumpyVectorIndex`에서 정규화를 생략하지 않는다. 모델을 바꾸면 달라질 수 있고 정규화는 적재 시 1회 비용이다.
**뒤집을 조건** Ollama가 `/api/embed`를 없애거나, 배치 요청에서 품질 차이가 측정으로 확인될 때.

### D-017 · Ollama 장애는 `Refusal`이 아니라 예외다
**결정** 외부 시스템 장애 예외를 `src/infrastructure/errors.py`에 둔다(`OllamaUnavailable`, `EmbeddingUnavailable`). `application`은 이 예외를 잡지 않는다. `interface`(CLI)까지 올라가게 둔다.
**근거** `Refusal`은 "근거가 없다" 또는 "안전 규칙 위반"을 뜻한다(D-007, SAFETY.md S-2·S-4). Ollama가 꺼진 것은 다른 종류의 실패다. 둘을 같은 타입으로 표현하면 서버 장애 상황에서 사용자에게 "관련 자료를 찾지 못했습니다"라고 잘못 안내하게 된다. 사용자는 질문을 바꿔 가며 재시도하게 되고 원인은 드러나지 않는다.
**구현** 어댑터가 `requests.RequestException`을 잡아 `EmbeddingUnavailable`로 바꾼다. 예외 메시지에 입력 텍스트를 넣지 않는다. 모델 이름과 건수만 넣는다.
**뒤집을 조건** 없음.

---

### D-018 · 색인은 `.npz` 파일 하나에 저장한다
**결정** `NumpyVectorIndex.save`는 파일 하나만 만든다. 벡터는 `vectors` 배열, 청크 메타데이터는 같은 파일의 `chunks` 키에 JSON 문자열로 넣는다. 읽기는 `np.load(path, allow_pickle=False)`로 한다.
**근거** `docs/guidelines/03-infrastructure.md`의 초안은 "청크 메타데이터는 별도 JSON"이었다. 파일이 둘이면 한쪽만 복사되거나 한쪽만 갱신된 상태가 생기고, 그 상태의 색인은 벡터와 청크가 어긋난 채로 검색된다. 잘못된 근거가 답변에 붙는 경로다. 포트의 시그니처도 `save(path: Path)` 하나라 파일 하나가 자연스럽다. `allow_pickle=False`는 색인 파일을 읽는 것만으로 코드가 실행되는 것을 막는다.
**구현** `np.savez_compressed(path, vectors=..., chunks=json_str)`. 로드 시 두 키의 존재와 개수 일치를 확인하고, 어긋나면 `ValueError`를 낸다. 빈 색인은 저장을 거부한다. 적재 실패를 성공으로 착각하게 되기 때문이다.
**뒤집을 조건** 색인이 수 GB가 되어 메타데이터만 따로 빠르게 읽어야 할 때. 현재 규모(9,377 × 768, 약 27MB)에서는 해당 없다.

---

### D-019 · 근거 충분성은 임계값 이상 근거의 존재 여부로 판정한다
**결정** `RetrievalPolicy.select()`가 `min_score` 이상인 근거만 고르고, `is_sufficient()`는 그 결과가 비었는지만 본다. 프롬프트에 넣는 근거와 `Answer.evidence`도 같은 목록이다.
**근거** 판정 기준이 하나여야 M3-3에서 임계값 하나만 움직여 측정할 수 있다. 임계값 미만 문단을 프롬프트에 넣지 않는 이유는 두 가지다. 관련 없는 문단이 생성 모델을 흔든다. 그리고 `Answer.evidence`는 사용자에게 근거로 제시되므로, 점수가 낮은 문단이 섞이면 답변과 무관한 문단을 근거라고 보여 주게 된다.
**구현** 정렬을 전제하지 않는다. 색인이 점수 내림차순으로 주더라도 그 약속에 기대면 색인 구현을 바꿀 때 조용히 깨진다.
**뒤집을 조건** "임계값 이상 n건" 같은 기준이 실데이터에서 더 나은 것으로 측정되면 (M3-3).

---

### D-020 · "자료에 답이 없다"는 신호는 고정 토큰으로 받는다
**결정** 프롬프트가 모델에게 `NO_ANSWER` 한 줄만 출력하라고 지시하고, `AnswerQuestion`이 그 토큰을 보면 `Refusal(NO_EVIDENCE)`로 처리한다. 토큰을 어기고 한국어로 풀어 쓴 경우를 잡는 보조 정규식을 함께 둔다.
**근거** 처음에는 한국어 문장("제공된 자료에 해당 내용이 없습니다")을 시켰다. `qwen2.5:3b`가 문장을 바꿔 써서 문자열 비교가 실패했고, 근거 없는 답이 `Answer`로 나갔다 (ERRORS.md E-002). 자연어는 모델이 바꿔 쓴다. 기계가 판정할 신호는 짧고 형태가 고정된 토큰이어야 한다.
**구현** 보조 정규식은 주어를 "자료·문단·문서"로 제한한다. "없습니다"만 보면 "알코올이 없습니다" 같은 정상 답변까지 거부한다.
**뒤집을 조건** 생성 모델을 바꿔 토큰 준수율이 떨어지면 다른 신호 방식을 검토한다.

---

### D-021 · min_score=0.35는 현재 게이트로 동작하지 않는다. 실데이터로 다시 정한다
**결정** 임계값 0.35를 그대로 두되, 이것이 무관한 질문을 걸러 내지 못한다는 사실을 기록한다. 조정은 실데이터 Recall 측정(M2-6) 이후 M3-3에서 한다.
**근거** 샘플 문서 10건(청크 20건)으로 실측했다 (2026-09-08, `nomic-embed-text`).

| 질문 | 상위 1건 코사인 |
|---|---|
| 민감성 피부 메이크업 주의점 | 0.912 |
| 염증성 피부 턱선 도구 | 0.897 |
| 파이썬 리스트 정렬 방법 | 0.892 |
| 자동차 엔진 오일 교환 주기 | 0.842 |
| 오늘 서울 날씨 | 0.827 |

무관한 질문도 0.83~0.89다. 0.35 게이트는 절대 걸리지 않는다. 참고로 `nomic-embed-text`가 문서에 요구하는 `search_document: ` / `search_query: ` 접두사를 붙여 다시 재면 관련 0.806~0.843, 무관 0.720~0.803으로 값이 내려가지만 구간이 여전히 겹친다.
**현재 상태** 무관한 질문은 검색 게이트가 아니라 생성 결과 판정(D-020)에서 걸린다. 실제 스택으로 확인했다(`tests/application/test_answering_end_to_end.py`). 다만 이 경로는 모델의 지시 준수에 기댄다. SAFETY.md S-2가 요구하는 "생성 전에 막는다"는 아직 형식만 갖춰져 있다.
**다음 행동** (1) M2-6에서 실데이터 QA쌍으로 관련·무관 질문의 점수 분포를 만든다. (2) 접두사 도입 여부를 그 분포로 판단한다. (3) M3-3에서 임계값을 정한다.
**뒤집을 조건** 없음. 측정으로 값을 갱신한다.

---

### D-022 · 생성 temperature 기본값은 0.0이다
**결정** `OllamaGenerator`의 기본 `temperature`를 0.0으로 둔다.
**근거** 같은 질문에 같은 답이 나와야 기준선(M2-8) 대비 개선을 판단할 수 있다. 생성이 흔들리면 점수 차이가 변경 때문인지 표집 때문인지 구분되지 않는다.
**뒤집을 조건** 답변이 지나치게 짧거나 반복적이어서 품질이 떨어지는 것이 측정되면 M3-4에서 조정한다.

---

### D-023 · `src/cli.py`는 얇은 진입점이고 본체는 `src/interface/cli.py`다
**결정** `src/cli.py`에는 `from src.interface.cli import main`과 `raise SystemExit(main())`만 둔다. argparse 정의와 출력 형식은 `src/interface/cli.py`에 있다.
**근거** 두 문서가 서로 다른 것을 요구했다. `CLAUDE.md`와 `README.md`는 명령을 `python -m src.cli`로 적었고, `docs/ARCHITECTURE.md` 4절과 레이어 규칙은 진입점이 `interface` 아래 있어야 한다고 정한다. 명령을 바꾸면 문서 세 곳을 고쳐야 하고 사용자가 외운 명령이 달라진다. 얇은 진입점 하나로 둘 다 지킨다.
**구현** `src/cli.py`는 레이어 디렉터리 밖이라 `scripts/check_layers.py`가 검사하지 않는다. `src/composition.py`와 같은 위치다.
**뒤집을 조건** 진입점이 여럿이 되면(예: FastAPI 추가, M4-4) 이 배치를 다시 본다.

---

### D-024 · 데이터 검사 도구는 집계값만 출력한다
**결정** `scripts/inspect_dataset.py`는 파일 형식, 용량, 키 경로, 길이 통계, 저빈도 라벨 값만 출력한다. 30자를 넘는 문자열 값은 길이만 세고, 30자 이하라도 고유값이 20종을 넘으면 나열하지 않는다.
**근거** M2-1은 원본을 읽어야 답할 수 있는 질문이다. 그런데 원문이 터미널 출력과 세션 로그에 남으면 그것이 유출 경로가 된다 (D-006, SAFETY.md S-6). 값의 길이와 종류 수만으로 필드 구조를 파악할 수 있으므로 원문을 볼 이유가 없다.
**구현** 이 규칙을 테스트로 고정했다(`tests/scripts/test_inspect_dataset.py`의 "원문 유출 방지" 절). 긴 값이 보고서에 나타나면 테스트가 깨진다.
**훅과의 관계** `scripts/guard_data.py`는 셸 명령 문자열에 `data/` 경로가 보이면 차단한다. 이 도구는 기본 경로를 코드 안에 두어 명령에 경로를 적지 않는다. 훅이 막으려는 것은 원문이 대화로 새는 것이고, 이 도구는 집계값만 내므로 그 목적에 어긋나지 않는다.
**뒤집을 조건** 없음. 검사 도구를 늘릴 때도 같은 제약을 적용한다.

---

### D-025 · 평가 입력 형식을 먼저 정하고, AI Hub QA쌍은 그 형식으로 변환한다
**결정** 평가 입력은 이 프로젝트가 정한 형식(`eval/qaset.json`)이다. AI Hub QA쌍 10,043쌍은 M2-5에서 이 형식으로 변환한다. 채점기는 원본 형식을 모른다.

```json
[{"id": "Q-1", "question": "...", "expected": "...", "gold_doc_ids": ["sample-001"]}]
```

**근거** 원본 필드명을 모르는 상태(M2-1 미완료)에서도 채점기를 만들 수 있다. 그리고 원본 스키마가 바뀌거나 다른 평가셋을 쓰게 돼도 고칠 곳이 변환기 하나로 남는다. 로더가 원본 형식을 직접 읽으면 스키마 변화가 채점 코드까지 번진다.
**Recall@k 정의** 상위 k개 검색 결과의 문서 ID 중 정답 문서 ID가 하나라도 있으면 hit로 센다. 정답 문서가 여럿일 때 몇 개를 찾았는지는 세지 않는다. 이 데이터셋의 QA쌍은 질문 하나에 근거 문단 하나가 대응할 가능성이 높고(확인 필요, M2-1), 부분 점수를 도입하면 해석이 복잡해진다.
**채점 불가 처리** `gold_doc_ids`가 없는 케이스는 Recall 계산에서 뺀다(`EvalCase.is_gradable`). 0점으로 세면 점수가 실제보다 낮게 나온다. 보고서에 `sample_count`와 `graded_count`를 함께 남겨 몇 건이 빠졌는지 보이게 한다.
**답변 정확도** 아직 넣지 않는다. 채점 방식이 M2-7에서 정해진다. 방식을 정하기 전에 아무 숫자나 넣으면 그것이 기준선이 된다.
**뒤집을 조건** 실데이터 QA쌍이 근거 문단을 여럿 지목하고 그 개수가 의미를 가지면 부분 점수를 검토한다.

---

### D-026 · 답변 정확도는 음절 2-gram F1로 잰다 (M2-7)
**결정** 주 지표는 기대 답변과 생성 답변의 **음절 2-gram F1**이다(`src/domain/scoring.py`). 보조 지표로 임베딩 코사인을 함께 기록한다. 표본은 **무작위 200건, seed 42 고정**이다.

**왜 음절 2-gram인가** 한국어는 조사가 붙어 어절 비교가 쉽게 어긋난다. "피한다"와 "피하고"는 어절로는 완전히 다르지만 음절 2-gram으로는 "피하"를 공유한다. 형태소 분석기를 쓰면 더 정확하지만 새 의존성이고(`docs/PACKAGES.md` 승인 대상) 학습 목적에 비해 무겁다.

**왜 F1인가** 재현율만 쓰면 길게 쓸수록, 정밀도만 쓰면 짧게 쓸수록 유리해진다.

**검토했으나 쓰지 않은 것**

| 방식 | 쓰지 않는 이유 |
|---|---|
| LLM-as-judge (`qwen2.5:3b`) | 케이스당 수 초. 표본 200건이면 채점에만 20분 가까이 들고, 3B 모델의 채점 판단을 신뢰할 근거가 없다. 비결정적이라 재현도 안 된다 |
| 임베딩 코사인 단독 | D-021에서 무관한 질문도 0.83이 나왔다. 변별력이 의심스럽다. 보조 지표로만 쓴다 |
| 수동 채점 | 가장 정확하지만 M3에서 실험마다 반복할 수 없다 |

**거부 처리** 거부한 케이스는 정확도 계산에서 뺀다. 거부는 틀린 답이 아니라 답하지 않은 것이고, 거부율로 이미 세고 있다. 두 지표를 섞으면 같은 실패를 두 번 세게 된다(D-008).

**표본** 10,043쌍 전량은 생성만으로 여러 시간이 걸린다(샘플 실측 기준 케이스당 약 5초). 200건에 seed를 고정한다. seed를 고정하지 않으면 점수 차이가 변경 때문인지 표본 때문인지 구분되지 않는다. 검색만 재는 실행(`--skip-answers`)은 전량으로 돌려도 된다.

**한계** 표현이 다르고 뜻이 같은 답은 낮게 나온다. 샘플 실측(2026-09-08)에서 F1 0.348, 코사인 0.843이었다. 생성 답변이 기대 답변보다 훨씬 길어 정밀도가 떨어진 결과다. **그래서 이 점수는 절대 품질이 아니라 기준선 대비 비교용이다.** 합격/불합격 임계값은 두지 않는다. 기준선(M2-8)이 생기기 전에 임계값을 정하면 근거 없는 숫자가 기준이 된다.

**뒤집을 조건** 기준선 이후 F1이 사람 판단과 크게 어긋나는 것이 확인되면 형태소 분석기 도입이나 LLM-as-judge를 다시 검토한다. 그때 `docs/PACKAGES.md`에 근거를 적고 승인을 받는다.

---

---

### D-027 · 색인 대상은 QA 라벨링의 근거 발췌다
**결정** RAG 색인에 넣는 것은 QA 라벨링 JSON의 `Source_info1~5.Knowledge Data1~5`(고유 5,921건, 평균 170자)다. 지식데이터 `.doc` 6,170건의 본문은 지금 색인하지 않는다.
**근거** 세 가지다. (1) `.doc`이 한 형식이 아니다. OLE2 5,446 / ZIP 718 / RTF 4 / 빈 파일 2로 섞여 있어 변환 수단이 따로 필요하고, 그것은 새 의존성이다(`docs/PACKAGES.md` 승인 대상). (2) 지식데이터 `.json`에는 본문이 없다. 메타데이터뿐이다. (3) 발췌는 질문과 같은 파일 안에 있어 Recall@k의 정답 연결이 확실하다. 파일명 대조 같은 추정이 필요 없다.
**한계** 본문 전체가 아니라 발췌만 검색된다. 발췌가 평균 170자라 청킹이 거의 일어나지 않아 M3-1(청크 크기 실험)의 의미가 약해진다.
**뒤집을 조건** ~~M3에서 `.doc` 원문 색인을 실험 항목으로 만들어 기준선과 비교한다.~~ **끝났다(M3-7).** 표준 라이브러리로 변환 수단을 만들어 비교했고 논문 원문은 F1 -0.0809로 기각됐다. 근거는 D-035다. 발췌만 쓰는 결정은 유지되지만, 실제 색인 재료는 D-032에 따라 QA쌍이다.

---

### D-028 · 피부 유형은 4대 분류와 세부 유형을 함께 붙인다
**결정** 청크 메타데이터에 `SkinType`(4대 분류)과 세부 유형 문자열(아토피 피부, 홍조 피부, 지루성 피부염 등 13종)을 둘 다 붙인다. 출처는 폴더명이다(`TL_민감성 피부_아토피 피부`).
**근거** 4대 분류만 남기면 세부 유형 정보가 사라진다. M3-2의 메타데이터 필터링을 세부 유형 단위로도 실험할 수 있어야 한다. `SkinType` Enum은 그대로 두고 세부 유형은 자유 문자열로 받는다. 세부 유형을 Enum으로 만들면 13종이 고정되고, 실데이터에 다른 표기가 나오면 적재가 통째로 실패한다.
**뒤집을 조건** 세부 유형이 검색 품질에 아무 영향이 없는 것이 M3-2에서 측정되면 필드를 뺀다.

---

### D-029 · Recall@k를 기준선 지표에서 뺀다
**결정** 이 데이터셋의 주석된 근거 링크로는 Recall@k를 측정하지 않는다. 기준선(M2-8)은 답변 정확도(음절 2-gram F1)와 거부율로 잡는다. 검색 채점 코드(`EvaluateRetrieval`)는 남겨 두되 기준선에서 뺀다.

**근거** 실측(2026-09-08, 실데이터 9,031케이스 / 5,930청크).

| 측정 | 값 |
|---|---|
| 전량 Recall@5 (`nomic-embed-text`) | 0.001 (13/9,031). 무작위 기대 0.0008 |
| 550문서 코퍼스 Recall@5 | nomic 0.0%, bge-m3 2.0% (무작위 기대 0.9%) |
| 질문 ↔ 정답 근거 코사인 | 0.603 |
| 질문 ↔ 무관 근거 코사인 | 0.606 |
| 답변 ↔ 정답 근거 어휘 겹침(F1) | 0.062 (무관 근거 0.027) |

**정답 근거가 무관한 근거보다 질문에 가깝지 않다.** 모델을 바꿔도(bge-m3) 무작위를 겨우 넘는다. 색인은 정상이다. 근거 원문을 질의로 넣으면 자기 자신이 1위로 유사도 1.000이 나온다.

원인은 링크 구조다. 질문은 350자짜리 페르소나 서술이고, 근거는 출처에서 뽑은 짧은 사실 조각이다. 주석자가 **답변을 뒷받침하려고** 붙였기 때문에 질문에서 근거로 가려면 답변을 거쳐야 한다. 검색 하이퍼파라미터로 좁힐 수 있는 간격이 아니다.

**남는 것** 답변 정확도(D-026), 거부율, 평균 최고 유사도. 검색 변경(M3-1, M3-2, M3-3)의 효과는 이 세 지표로 간접 측정한다.
**뒤집을 조건** 사람이 직접 gold를 붙인 평가셋을 만들면(질문 100건 규모) 그때 Recall을 되살린다.

---

### D-030 · k=3을 기본값으로 한다. 유사도 임계값은 조정하지 않는다
**결정** `RetrievalPolicy.k`를 5에서 3으로 바꾼다. `min_score`는 0.35에 둔다. 임계값 조정을 개선 수단으로 쓰지 않는다.

**k 근거** 같은 200건에서 측정했다. 비교는 양쪽 모두 답한 케이스만 본다(`scripts/compare_eval.py`).

| 비교 | F1 차이 | 표준오차 | 개선/악화 |
|---|---|---|---|
| k=5 → k=3 | +0.0053 | 0.0031 | 111 / 85 |
| k=3 → k=1 | -0.0004 | — | — |
| k=5 → k=1 | +0.0053 | 0.0030 | 107 / 90 |

4~5번째 근거는 노이즈였다. 2~3번째는 점수에 기여하지 않는다. 사실상 1번째 근거만 기여한다. k=1과 k=3이 같은 점수라면 **사용자에게 보여 줄 근거가 많은 쪽**을 택한다. 근거 제시가 이 시스템의 존재 이유다(`docs/guidelines/04-interface.md`).

**임계값 근거** 실데이터 200건의 최고 유사도가 0.638~0.780에 뭉쳐 있다(중앙 0.688). 스윕 결과다.

| 임계값 | 거부율 | 남은 F1 |
|---|---|---|
| 0.35 | 0.000 | 0.2325 |
| 0.65 | 0.040 | 0.2318 |
| 0.70 | 0.625 | 0.2378 |
| 0.75 | 0.970 | 0.2359 |

거부만 늘고 F1은 움직이지 않는다. 점수에 관련성 신호가 없기 때문이다(D-029와 같은 뿌리). 임계값을 올리면 무관한 질문이 아니라 **아무 질문이나** 거부된다.

**남는 문제** SAFETY.md S-2("근거가 없으면 생성 전에 막는다")가 형식만 있고 실효가 없다. 유사도로는 근거의 유무를 판정할 수 없다. 거부는 현재 생성 모델의 자기 판단(`NO_ANSWER`)과 출력 안전 검사에만 의존한다 (O-1 갱신).
**뒤집을 조건** 검색 신호가 바뀌면(다른 임베딩 모델, 재랭킹, 다른 색인 구성) 임계값을 다시 잰다.

---

### D-031 · 메타데이터 필터링을 넣지 않는다. 개선 여지는 검색이 아니라 생성에 있다
**결정** 피부 유형 메타데이터로 검색을 좁히지 않는다(M3-2 기각). 질문에서 유형을 뽑는 추출기도 만들지 않는다. `skin_detail` 필드 자체는 남긴다. 출력에 표시되고 비용이 없다 (D-028).

**M3-2 근거** 질문자의 피부 유형을 **안다고 가정한** 오라클 필터로 상한을 쟀다. 40건을 과다 인출해 유형이 일치하는 문단만 상위 3건으로 좁혔다.

| 항목 | k=3 무필터 | 오라클 필터 |
|---|---|---|
| F1 (공통 198건) | 0.2380 | 0.2382 |
| 결과가 바뀐 케이스 | — | 0건 |
| 필터 적용 실패(폴백) | — | 39건 (19.5%) |

161건은 필터가 실제로 걸렸는데도 답이 한 건도 바뀌지 않았다. **상한이 0이므로 추출기를 만들 이유가 없다.**

**ablation — 검색은 작동한다** 근거를 무작위 문단 3건(정답 제외, seed 42)으로 치환했다.

| 항목 | 실제 검색 | 무작위 근거 |
|---|---|---|
| F1 (공통 166건) | 0.2372 | 0.2285 (-0.0087) |
| 거부 | 2건 | 33건 |
| `answered → refused` | — | 32건 |

모델은 근거를 읽는다. 무관한 문단을 주면 32건에서 `NO_ANSWER`를 낸다. 근거를 무시하고 일반론으로 답하는 것이 아니다.

**결론** 검색의 기여는 "답할지 말지"에 주로 나타나고 "무엇을 답하는지"에는 약하게 나타난다(-0.0087, 표준오차 약 0.003). 상위 3건 안에서 무엇을 넣든 모델에게는 비슷한 재료이기 때문이다. 그래서 M3의 검색 쪽 실험이 전부 미미했다(k 조정 +0.0053, 임계값 0, 메타 필터 +0.0001). **남은 개선 여지는 생성 쪽이다(M3-5).**
**뒤집을 조건** 색인 구성을 바꾸면(M3-6 QA 색인, M3-7 원문 색인) 검색 쪽 실험을 다시 연다. 재료가 달라지면 필터의 의미도 달라진다.

---

### D-032 · 색인 재료를 QA쌍으로 바꾼다. QA는 쪼개지 않는다
**결정** 색인에 넣는 것은 QA 라벨링 1건 전체(`질문 + 답변`)다. `AihubQaPairLoader` + `WholeDocumentChunker` 조합을 기본값으로 쓴다(`composition.DEFAULT_MATERIAL = "qa"`). 근거 발췌 색인(D-027)은 `--material excerpt`로 남긴다.

**근거** 같은 200건, 같은 조건에서 잰 값이다. 평가 200건은 색인에서 빼고 8,831건만 넣었다.

| 항목 | 발췌 색인 | QA쌍 색인 |
|---|---|---|
| F1 (공통 178건) | 0.2383 | **0.3148 (+0.0766)** |
| 거부 | 2건 | 20건 |

+0.0766은 표준오차(약 0.003)의 25배다. 이 프로젝트에서 가장 큰 폭이며, 검색 파라미터 실험(k, 임계값, 필터)이 전부 ±0.005 안에서 움직였던 것과 대비된다.

**왜 쪼개지 않는가** QA 텍스트가 평균 855자(질문 356 + 답변 499)라 512자 청커에 걸려 2~3조각이 된다. 그러면 상위 k를 같은 QA의 조각들이 차지한다. 실제로 첫 배포 색인(20,681청크)에서 근거 3건 중 2건이 같은 QA였다. 청커를 `WholeDocumentChunker`로 바꿔 9,031청크가 됐고 근거가 서로 다른 QA로 채워졌다.

**한계 두 가지**
1. **지표가 유리하다.** QA 색인은 같은 데이터셋의 답변 문장을 근거로 넣는다. 기대 답변과 어휘·문체가 애초에 가깝고, 음절 2-gram F1은 그것을 그대로 점수로 본다. 실제 유용성 개선인지 지표 편향인지는 사람이 읽어 봐야 갈린다 (O-4).
2. **실사용 색인에는 평가 케이스가 들어간다.** 데이터셋에 있는 질문과 똑같이 물으면 그 답을 그대로 돌려준다. FAQ 검색으로는 정상이지만 성능 수치는 그만큼 낙관적으로 읽힌다.
**뒤집을 조건** 사람이 읽고 채점한 표본에서 발췌 색인이 더 낫다고 나오면 되돌린다.

---

### D-033 · 생성 모델은 qwen2.5:3b를 유지한다
**결정** `qwen2.5:14b`로 바꾸지 않는다. VRAM에는 들어가지만(9.0GB) 지표가 나빠졌다.

| 항목 | qwen2.5:3b | qwen2.5:14b |
|---|---|---|
| F1 (공통 169건) | 0.2376 | 0.2016 (-0.0361) |
| 거부 | 2건 | 29건 |
| 임베딩 코사인 | 0.811 | 0.765 |

**단서** 14b의 거부 29건은 "근거가 질문에 안 맞는다"는 판단일 수 있다. 그렇다면 더 정직한 행동이고, F1은 그것을 벌점으로 센다. 즉 "14b가 나쁘다"가 아니라 "14b는 이 지표에서 불리하다"가 정확한 표현이다 (O-4와 같은 뿌리).
**뒤집을 조건** 답변 정확도 지표를 사람 판단 기반으로 바꾸면 다시 잰다.

---

### D-034 · F1은 실제 품질보다 비관적이다. 안전 규칙에 의료 이용 판단을 추가한다
**배경** 모든 판정이 음절 2-gram F1 하나에 걸려 있는데 그 지표를 사람 판단과 대조한 적이 없었다 (O-4). 로컬 웹 UI(`python -m src.cli serve`)를 만들어 실사용으로 확인했다.

**사람 판정 30건** (사용자가 직접 질문하고 O/△/X)

| 구분 | 건수 | 판정 |
|---|---|---|
| 답변 | 22 | **O 22 / △ 0 / X 0** |
| 거부 | 8 | O 1 / △ 3 / X 4 |

**규칙 판정 15건** (유형을 아는 합성 질문으로 재측정)

| 유형 | 결과 |
|---|---|
| 정상 10건 | **답변 10 / 거부 0.** 최고 유사도 0.62~0.79, 153~372자 |
| 진단요구 5건 | 거부 4, 답변 1 (병원 방문을 권하는 내용. 안심 단정 없음) |

**결론 1 — 시스템 동작은 정상이다.** 정상 질문은 하나도 거부되지 않았다. 사용자가 거부 8건에 매긴 △ 3 / X 4는 무관·진단 요구 질문의 거부였고, "답을 안 해 준다"를 나쁘게 본 판정 기준 차이다. 그 거부들은 SAFETY.md S-2가 요구하는 동작이다.

**결론 2 — F1은 실제 품질을 과소평가한다.** 같은 시스템이 F1 0.315인데 사람 판정으로는 답변 22건 전부 O였다. F1은 기대 답변과의 어휘 겹침만 보므로, 표현이 다르면서 질문에 제대로 답한 경우를 벌준다. **F1로 기각한 실험(D-033 14b, M3-4 길이 제약)은 재검토 여지가 있다.** 다만 지표를 실험 중에 바꾸지 않는다는 원칙에 따라 지금까지의 판정은 유지한다.

**추가한 안전 규칙** `SafetyPolicy`에 "의료 이용 판단"을 넣었다. `병원 안 가도 됩니다`, `심각한 상태는 아닙니다`, `2주면 좋아집니다` 계열을 막는다. 진단을 하지 않아도 진료를 미루게 만들 수 있다. 병원을 권하는 문장과 면책 문구는 통과시킨다. 이 규칙이 없었다면 걸렀어야 할 사례는 이번 실측에 없었다. 예방적 추가다.

**판정 기록 설계** `eval/raw/human-check.jsonl`에 판정·시각·질문 유형·근거 청크 ID·유사도·답변 길이만 남긴다. 질문과 답변 원문은 남기지 않는다 (S-7). 질문 유형(정상·무관·진단요구·미분류)은 분류값이라 원문이 아니다. 이 태그가 없어 1차 30건에서 결론을 못 냈다.
**뒤집을 조건** 사람 판정 표본이 100건 규모로 늘고 F1과의 상관이 실제로 높게 나오면 결론 2를 조정한다.

---

### D-035 · 논문 원문을 색인 재료로 쓰지 않는다
**결정** 지식데이터 `.doc` 원문 색인을 기각한다. QA쌍 색인(D-032)을 그대로 둔다. 추출 코드와 색인 경로는 남긴다.

**측정** (2026-09-10, 같은 표본 200건 / seed 42 / k=3 / min_score 0.35)

| 지표 | QA쌍 색인 | 논문 원문 색인 | 차이 |
|---|---|---|---|
| 답변 정확도 F1 (공통 175건) | 0.3162 | 0.2353 | **-0.0809** |
| 거부 | 20건 | 1건 | -19 |
| 생성 실패 | 1건 | 5건 | +4 |
| 최고 유사도 평균 | — | 0.7187 | — |

**색인 구성** `사용논문` 1,315건, 청크 19,673개(문서당 15.0개), 94.5 MB. 적재 실패 0건. 청크는 2,048자로 키웠다. 512자로 자르면 20만 청크가 되어 임베딩만 3시간이다.

**해석** 논문은 **질문에 답하지 않는다.** 실험 방법과 선행 연구 서술이 대부분이라 "이럴 땐 이렇게 하라"가 없다. 거부가 20건에서 1건으로 준 것이 방향을 보여 준다. 논문에는 어떤 질문에든 어휘가 겹치는 문단이 있어 유사도 0.35를 항상 넘긴다. 근거는 늘 찾지만 답은 더 나빠진다. **검색이 되는 것과 답할 수 있는 것은 다르다.**

D-032의 결론이 재료를 바꾼 두 번째 실험에서도 확인됐다. 질문에 직접 답한 사례가 가장 좋은 재료다.

**얻은 것** O-7이 닫혔다. `.doc` 6,237건을 표준 라이브러리로 읽는다(`src/infrastructure/doc_reader.py`). 새 의존성 0개.

**뒤집을 조건** 논문에서 답변 성격의 절(초록, 결론)만 뽑아 색인하면 결과가 달라질 수 있다. 지금은 본문 전체를 균일하게 잘랐다. 또는 QA쌍 색인과 논문 색인을 함께 검색하는 구성(M4-3 재랭킹과 함께)에서 다시 본다.

---

### D-036 · LlamaIndex를 도입하지 않는다 (측정으로 확인)
**결정** D-003을 유지한다. 같은 RAG를 LlamaIndex로 다시 만들어 재 봤고, 바꿀 이유가 나오지 않았다. 실험 코드는 `experiments/m4-2-llamaindex/`에 남긴다.

**측정** (2026-09-10, 양쪽 다 같은 날 코드 / 표본 200건 / seed 42 / 평가 케이스는 색인에서 제외)

| 항목 | 직접 구현 | LlamaIndex 0.14.24 |
|---|---|---|
| 답변 정확도 F1 (공통 171건) | 0.3210 | 0.3182 (**-0.0028**) |
| 색인 빌드 (8,831건) | 6.3분 | 6.7분 |
| 색인 크기 | **37.5 MB** | 167 MB |
| 색인 파일 수 | 1개 | 5개 |
| 2~6단계 코드 | 568줄 | 약 135줄 |
| 런타임 의존성 | 2개 | 70개 |

**결론 1 — 품질은 같다.** -0.0028은 측정 노이즈(O-10, ±0.005) 안이다. 검색 점수는 소수점 4자리까지 일치했다(같은 질문에 0.8457 / 0.8424 / 0.8421). 두 파이프라인은 같은 일을 한다.

**결론 2 — 코드는 준다.** 568줄이 135줄이 된다. 다만 줄어든 것의 정체를 봐야 한다. 우리 568줄에는 프레임워크가 대신해 주지 않는 것이 섞여 있다. 포트 정의 17줄(의존 역전), 색인 160줄(재개·체크포인트·응답 검증), 임베딩 어댑터 69줄(배치·형식 검증). 프레임워크를 써도 재개와 검증은 직접 붙여야 한다.

**결론 3 — 비용은 는다.** 의존성 2개가 70개가 된다. 들어오는 것 중에 `SQLAlchemy`, `nltk`, `pillow`, `tiktoken`이 있다. 우리는 Ollama만 쓰는데 OpenAI 토크나이저와 이미지 라이브러리가 따라온다. **런타임 의존성에 `pytest`와 `pytest-asyncio`가 들어 있다.** 색인 크기 4.5배는 벡터를 JSON 텍스트로 저장하기 때문이다. 쓰지 않는 `image__vector_store.json`과 `graph_store.json`도 생긴다.

**학습 목적으로는 직접 구현이 이긴다.** D-001이 이 프로젝트의 목적이다. `VectorStoreIndex(nodes, embed_model=...)` 한 줄 뒤에 임베딩·정규화·저장이 숨는다. 우리 코드에서는 그 셋이 각각 보인다.

**한계 — 프롬프트를 완전히 맞추지 못했다.** LlamaIndex 쪽 프롬프트를 다시 짜면서 우리 `build_prompt`와 다르게 만들었다. 머리말 `제공된 문단:`이 빠지고, 문단 표시가 `[문단 N] <청크ID, 피부유형, 부위>`가 아니라 `[근거 N]`이며, 끝의 `답변:` 유도가 없다. **그래서 거부 26건 대 5건 차이는 프레임워크 차이로 볼 수 없다.** F1은 양쪽 다 0.32라 결론이 흔들리지 않지만, 거부율 비교는 이 실험에서 얻지 못했다.

**뒤집을 조건** 재랭킹, 하이브리드 검색, 다중 색인 라우팅처럼 직접 짜면 수백 줄인 기능이 필요해질 때. 그때는 M4-3에서 다시 본다.

---

## 미해결 숙제

결정은 났지만 아직 닫히지 않은 것들이다. 해당 단계에 도달하면 여기부터 본다.

| ID | 내용 | 근거 | 언제 닫히는가 |
|---|---|---|---|
| O-1 | **검색 게이트(S-2)에 실효가 없다.** 임계값으로는 근거 유무를 판정할 수 없다(D-030 스윕) | D-021, D-030 | 검색 신호 자체를 바꿔야 한다. M3-2(메타 필터), M3-6(QA 색인), M4-3(재랭킹) |
| O-8 | 근거 발췌 색인의 답변 F1이 0.229로 낮다. QA쌍 색인은 F1 0.298이지만 거부율 50% | D-027 | M3-4에서 프롬프트를 완화한 뒤 QA 색인을 실험 항목으로 비교 |
| O-9 | 생성 요청 타임아웃. 200건 중 7~8건. 답변을 짧게 시키면 1건으로 줄었다 | E-003 | 생성 길이가 원인일 가능성이 커졌다. M3-5 모델 교체에서 함께 본다 |
| O-10 | 같은 프롬프트·seed·temperature 0인데 F1이 0.2372와 0.2363으로 다르다 | — | 실험 판정에 ±0.005 이내 차이는 노이즈로 본다. 반복 측정 여부는 M3 후반에 결정 |
| O-3 | 근거를 맞게 찾고도 생성이 `NO_ANSWER`를 낸 사례가 샘플 8건 중 2건 | D-020 | M3-4 프롬프트 구조 변경에서 확인 |
| O-4 | **F1이 실제 품질보다 비관적이다.** 사람 판정 22/22 O 대 F1 0.315 | D-026, D-034 | 표본 30건으로는 상관을 못 잰다. 100건 규모나 다른 지표가 필요하다 |
| O-5 | 거부된 케이스 ID를 평가 보고서에 남기지 않는다. 어느 질문이 거부됐는지 되짚을 수 없다 | — | M2-6에서 `eval/raw/` 설계와 함께 정한다 |
| ~~O-11~~ | ~~거부 사유 두 가지가 한 이름으로 합쳐진다. 검색 실패와 생성 실패를 구분할 수 없다~~ | M4-2 | **해소(2026-09-10).** `RefusalReason.NO_ANSWER_FROM_MODEL`을 추가했다. 이제 보고서에서 검색 실패와 생성 실패가 갈린다. 과거 결과 파일의 라벨은 고치지 않는다 |
| ~~O-7~~ | ~~`.doc` 원문을 색인하지 않는다. OLE2 변환 수단이 없다~~ | D-027 | **해소(2026-09-09).** 표준 라이브러리로 읽는다(`src/infrastructure/doc_reader.py`). 실측 6,237건 전량 추출 성공, 새 의존성 0개 |

---

## 세션 로그

무엇을 했는지 한 줄씩만 남긴다. 상세는 `docs/ROADMAP.md`의 마일스톤별 결과에 있다.

| 날짜 | 내용 |
|---|---|
| 2026-09-07 | 하네스 문서 골격, 훅 2종, 의존성, Ollama 확인. D-001~011. E-001로 생성 모델을 `qwen2.5:3b`로 교체 |
| 2026-09-07 | M1-1~M1-4. 값 객체, 포트, 로더·청커, 임베더. D-012~017 |
| 2026-09-08 | M1-5 색인. D-018 |
| 2026-09-08 | M1-6, M1-7 답변과 거부. E-002 발생. D-019~022 |
| 2026-09-08 | M1-8 CLI. **M1 완료.** D-023 |
| 2026-09-08 | M2-1 검사 도구 작성(데이터 대기). D-024 |
| 2026-09-08 | M2-5 평가 형식과 채점기. D-025 |
| 2026-09-08 | M2-7 답변 정확도 지표 확정. D-026 |
| 2026-09-08 | `MEMORY.md`·`docs/ROADMAP.md` 역할 분리 재구성. 미해결 숙제 `O-#` 신설. `handoff.md`와 `process-docs` 스킬을 새 구조에 맞춤 |
| 2026-09-08 | 데이터 수령. `scripts/extract_dataset.py`로 27개 zip 해제(18,408개 파일). **M2-1 완료** — `.doc` 형식 혼재 확인. D-027, D-028 |
| 2026-09-08 | **M2-2·M2-3·M2-5 완료**, M2-6에서 Recall 측정 불가 판정. 임베딩을 bge-m3로 교체. D-005 개정, D-008 개정, D-029 |
| 2026-09-08 | M2-4 완료(2,000건 체크포인트, `--resume`). E-003 발생 — 생성 타임아웃 1건이 200건 평가를 죽여 부분 실패를 견디게 고쳤다 |
| 2026-09-08 | **M2-8 완료. M2 종료.** 기준선 F1 0.237 / 거부율 0.057 / 생성 실패 3.5%. `eval/baseline.json` |
| 2026-09-08 | M3-4 길이 제약 실험 → F1 기준 기각(-0.0031, 노이즈). O-5 해결(케이스별 기록 + `scripts/compare_eval.py`). O-10 신설 |
| 2026-09-08 | M3-3 완료. k=5→3 채택(+0.0053). 임계값 조정은 무효로 판정. D-030 |
| 2026-09-09 | M3-2 기각(오라클 필터 +0.0001). ablation으로 검색 기여 확인(무작위 근거 시 거부 2→33건). D-031 |
| 2026-09-09 | **M3-6 채택** — QA쌍 색인 F1 0.238→0.315. M3-5 기각(14b가 -0.036). 못 쓰는 모델 2종 삭제(42GB). D-032, D-033 |
| 2026-09-09 | 로컬 웹 UI(`serve`) 추가. 실사용 45건 확인 — 정상 질문 거부 0건, F1이 실제 품질보다 비관적. 안전 규칙 5번째 추가. D-034 |

---

## 항상 참인 사실

- 사용자는 Unity/C# 실무자다. Python과 RAG는 처음이다.
- 대상 데이터는 실존 인물의 피부 상태를 다루는 지식이다. 가볍게 다루지 않는다.
- 하드웨어는 RTX 4070 Ti SUPER 16GB, RAM 64GB, Windows 11이다. WSL 배포판은 설치돼 있지 않다.
- **VRAM 16GB를 넘는 모델은 쓰지 않는다.** RAM 스필은 실사용 불가 수준으로 느리다 (E-001).
- WSL은 필요 없다. Git Bash에 GNU coreutils 8.32가 있어 AI Hub 분할 파일 병합이 가능하다.
- 출처 없는 수치를 쓰지 않는다.
